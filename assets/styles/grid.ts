import { StyleSheet } from 'react-native';
import { font } from './variables';
export default StyleSheet.create({
	container: {
		margin: 'auto',
		marginTop: 0,
		marginBottom: 0,
		width: '90%',
	},
});
